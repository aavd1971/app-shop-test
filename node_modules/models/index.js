var Mongoose = require('mongoose');
var mongoose = Mongoose.connect('mongodb://alex1971:aavd04121971@ds047030.mongolab.com:47030/app');
//var mongoose = Mongoose.connect('mongodb://localhost/app');
var Item = new mongoose.Schema({
    id: {type: Number},
    name: {type: String},
    qty: {type: Number},
    price: {type: Number}
},
    {
    collection: 'items'
    }
);
Item = mongoose.model('item',Item);
module.exports.Item = Item;
